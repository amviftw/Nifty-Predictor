from setuptools import setup, find_packages

setup(
    name="nifty_predictor",
    version="1.0.0",
    description="Nifty 50 directional movement prediction system",
    packages=find_packages(),
    python_requires=">=3.10",
)
